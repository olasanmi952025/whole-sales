{
    "$schema": "https://railway.app/railway.schema.json",
    "build": {
        "builder": "NIXPACKS",
        "buildCommand": "npm install && npm run build:backend && npm run copy:assets && cd web/frontend && npm install && npm run build"
    },
    "deploy": {
        "startCommand": "node dist/web/index.js",
        "restartPolicyType": "ON_FAILURE",
        "restartPolicyMaxRetries": 10
    }
}